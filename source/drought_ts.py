from load import * 
from pandas import Series 
import matplotlib.pyplot as plt

# droughts = parse_drought(save_weekly = False)

# drought_subset = droughts.loc[droughts['state'] == 'CA']

# fig, ax = plt.subplots()

# for key, grp in drought_subset.groupby(['county']):
#     ax = grp.plot(ax=ax, kind='line', x='valid_start', y='d1', label=key)
#     break

# plt.legend(loc='best', fontsize = 2)
# plt.show()
